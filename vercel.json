{
  "version": 2,
  "buildCommand": "pnpm --filter @rigs/web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://api.rigs.ru/api/:path*" }
  ]
}
